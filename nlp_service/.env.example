# Shared secret the backend must send (as the X-NLP-Api-Key header) to call
# /suggest. Generate one with, e.g., `openssl rand -hex 32` and put the same
# value in backend/.env as NLP_API_KEY. Required -- /suggest refuses all
# requests if this is unset.
NLP_API_KEY=
